{"params":[{"name":"prompt","type":"string","default":"","required":true,"what":"The text to speak, normalised the way you want it read.","alias":["text"]},{"name":"model","type":"string","default":"","required":true,"what":"Which model to run. Send <code>bodhan-tts</code>.","alias":[]},{"name":"voice_clone_id","type":"string","default":"\"Amit\"","required":false,"what":"Which of the 45 voices reads it.","alias":["speaker"]},{"name":"style","type":"string","default":"","required":false,"what":"Delivery register. Omit it for the conversational default.","alias":[]},{"name":"temperature","type":"float","default":"0.6","required":false,"what":"Sampling randomness. Lower is steadier, higher is more varied.","alias":[]},{"name":"top_p","type":"float","default":"0.9","required":false,"what":"Nucleus sampling cutoff.","alias":[]},{"name":"top_k","type":"int","default":"50","required":false,"what":"Sampling pool size.","alias":[]},{"name":"repetition_penalty","type":"float","default":"1.2","required":false,"what":"Discourages the model from repeating itself.","alias":["nvext.repetition_penalty"]},{"name":"max_tokens","type":"int","default":"2520","required":false,"what":"Hard ceiling on generated audio length.","alias":[]}],"caveats":[{"param":"prompt","title":"It is not required, and omitting it is not an error.","body":"A request with a missing or misspelled <code>prompt</code> returns <code>200</code> and a few seconds of audio rather than a <code>4xx</code>. Validate that the field is present and non-empty before sending."},{"param":"model","title":"It is not validated.","body":"Omitting it, or sending a model that does not exist, still returns <code>200</code>. Send the right value for correctness, but do not rely on a wrong one failing loudly."},{"param":"voice_clone_id","title":"The name is misleading: there is no voice cloning.","body":"It selects a library voice by name. There is no reference audio and no cloning, despite the field name. An unknown name returns <code>422</code>."},{"param":"style","title":"An unknown style is rejected, and the casing is exact.","body":"Styles are matched literally, so a display-cased string will not do. An unknown style returns <code>422</code>."},{"param":"top_p","title":"It applies only when strictly between 0 and 1.","body":"Sending <code>0</code> or <code>1</code> silently disables it rather than clamping."},{"param":"top_k","title":"It is not exclusive with <code>top_p</code>: both apply at once.","body":"Setting one does not switch the other off. Setting <code>top_k</code> to <code>1</code> makes generation greedy, which is the only way to get a byte-for-byte reproducible result: there is no seed parameter, so at the default temperature two identical requests return different audio."},{"param":"repetition_penalty","title":"A nested field can override it.","body":"<code>nvext.repetition_penalty</code> is accepted and takes precedence over the top-level value. Send one or the other, not both."},{"param":"max_tokens","title":"Reaching the ceiling truncates the audio without an error.","body":"The default is about thirty seconds of speech. Longer passages have to be split at punctuation and joined by the caller; the endpoint will not do it."},{"param":null,"title":"Unknown fields are ignored, not rejected.","body":"Any field outside this list (including <code>language</code> and <code>seed</code>) returns <code>200</code> and has no effect, so a typo in a parameter name is silently dropped. There is no language field at all; the language is inferred from the text."},{"param":null,"title":"Every client-side error arrives as <code>422</code>.","body":"The gateway collapses the backend&rsquo;s <code>4xx</code> responses into one status, and the real cause appears only in the message string. You cannot tell a bad voice from a bad style by status code; validate client-side against the voice and style lists. Saturation returns <code>429</code> or <code>503</code>, both retryable with backoff."}]}