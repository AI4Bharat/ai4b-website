Not in the build.

- `body-before-split.html` — the single-page body, as it stood before the post was
  split into the capability and technical pages on 2026-09-03. Kept so the split is
  reviewable; the live sources are `../body-capability.html` and `../body-technical.html`.
- `mapgen.py`, `map.json` — generated the voice atlas that was removed on 2026-09-03.
  See the header of `mapgen.py` for why the map went, and what is worth keeping from
  it (the point-in-polygon assert and the greedy label placement).
