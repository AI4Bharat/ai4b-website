# The hero letter animation (applied 2026-09-04)

The animation is live in `src/`; `python3 _undo_hero/UNDO.py` reverses every
recorded edit, newest first, and rebuilds, leaving unrelated edits alone.
Files here are the earlier drafts kept for reference: `hero-scripts-v3.js` +
`hero-ripple-on-disk.js` were the quieter first cut (one letter, caption, rings).
